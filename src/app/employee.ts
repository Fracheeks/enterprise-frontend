export class employee { 
    id: number;
    roleName?: string;
    username?: string;
    salary?: number;
}

/*! -> if you don't want to provide initial values for the fields
  ? -> parameters optional*/